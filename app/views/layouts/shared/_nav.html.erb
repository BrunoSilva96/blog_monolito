<nav class="navbar navbar-expand-lg bg-info">
  <div class="container px-4 px-lg-5">
    <a class="navbar-brand text-dark" href="<%= root_path %>">Blog Mono</a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
        <li class="nav-item"><a class="nav-link active text-dark" aria-current="page" href="<%= posts_path %>">Posts</a></li>
        <li class="nav-item"><a class="nav-link active text-dark" aria-current="page" href="<%= new_tags_path %>">Tags</a></li>
      </ul>
      <div class="d-flex">
        <% if user_signed_in? %>
          <div>
            <%= link_to ' Settings', settings_path, class: 'btn btn-outline-dark bi bi-gear-fill' %>
          </div>
          <div class='px-3' >
            <%= button_to ' Sair', destroy_user_session_path, class:'btn btn-outline-dark bi bi-box-arrow-right', method: :delete %>
          </div>
        <% else %>
          <%= link_to ' Login', new_user_session_path, class: 'btn btn-outline-dark bi bi-person-circle ' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>